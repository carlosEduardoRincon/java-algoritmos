package br.com.carlos.algoritmos.java.dataStructure;

/*
    O(n) - Read element, because to access the last element is necessary to iterate to each element
    O(1) - Add element, because just need to found anywhere free space in memory
         - Delete element, because just need to change the reference of one object to another
 */
public class LinkedList {
}
